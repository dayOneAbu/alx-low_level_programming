#include <stdio.h>

/**
* main - prints string with proper grammar, but the outcome is a piece
* of art,with new line
* Return: 0 if exited properly, non-zero otherwise
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
